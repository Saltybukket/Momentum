# Momentum handoff — 2026-07-12

## Checkout

- WSL distribution: `ISP2025`
- Repository: `/home/student/projects/Momentum`
- Branch: `codex/fix-scaffold-reproducibility`
- Starting HEAD for this run: `38d0f4d`
- Gate 1 commit: `54fd933 fix(identity): serialize guest recovery under concurrency`
- Gate 2 commit: `5399bc7 fix(backend): make idempotency transactional and expiring`
- Gate 3 commit: `c4e4680 fix(catalog): publish complete canonical catalog releases`.

## Completed gates

### Gate 0

- Docker Desktop is reachable from WSL (`Engine 29.6.1`, Compose `5.2.0`).
- PostgreSQL and Redis were healthy.
- GitHub Actions run `29177364961` for `38d0f4d` completed successfully.
- `services.gradle.org` was reachable and redirected correctly to the Gradle 8.13 distribution.
- Portable Gradle distribution: `/home/student/audit-artifacts/gradle-8.13-bin.zip`
- Windows copy for external audit handoff: `C:\Users\tfeic\Downloads\gradle-8.13-bin.zip`
- SHA-256: `20f1b1176237254a6fc204d8434196fa11a4cfb387567519c61556e8710aed78`
- This 130 MiB binary is intentionally outside Git and source archives. Supply it separately to an isolated auditor and point `distributionUrl` or its wrapper cache at the local file when outbound access is unavailable.

### Gate 1 — guest concurrency

- Create/recovery is serialized per installation.
- Same-process contention is rejected before work; PostgreSQL uses a transaction advisory lock.
- Recovery rotation uses compare-and-swap on the previous token hash.
- Twenty-request SQLite and PostgreSQL tests prove one successful credential, controlled `409`, no `500`, one user/session, and a usable winner token.
- OpenAPI documents `200`, `201`, `409`, and validation responses.
- ADR: `docs/adr/ADR-010-guest-auth-foundation.md`.

### Gate 2 — transactional idempotency

- Reservation, domain mutation, outbox row, response and completion share one UoW.
- Crash-before-completion regression proves complete rollback.
- Stored keys are `SHA-256(scope || NUL || key)`; scope binds method, route and principal.
- Bounded retention cleanup: `make idempotency-cleanup`.
- Alembic revision: `a91c4e2f6b70`.
- ADR: `docs/adr/ADR-011-transactional-idempotency.md`.

### Gate 3 — canonical catalog release

- Versioned JSON Schema: `data/schemas/catalog-release-v1.schema.json`.
- The canonical file contains a validated SHA-256 over sorted compact JSON excluding only `content_hash`; this avoids self-reference.
- Release metadata is persisted atomically with catalog data in `catalog_releases`.
- Snapshot is complete rather than capped at 100 and includes manifest metadata, `total`, completeness and ETag.
- List API uses an `items` plus `page` envelope with stable sorting and total count.
- Scale regressions cover 0, 3, 100, 101 and 488 records.
- Alembic head after this gate: `b72e5c8a104f`.

## Verified immediately before handoff

- Backend Ruff format/check and mypy: passed.
- Full backend suite with real PostgreSQL integration: 51 tests passed after the final hash-field consistency adjustment.
- Coverage: 75.90% (floor 70%).
- Fresh SQLite and PostgreSQL Alembic upgrade/check: passed; head `b72e5c8a104f`.
- Android `:data:compileDebugKotlin` and `:data:testDebugUnitTest`: passed.

## Do not start from stale assumptions

Run first:

```bash
cd /home/student/projects/Momentum
git status --short
git log -8 --oneline --decorate
cd fitness-platform/backend
uv sync --extra dev --frozen
uv run pytest
```

Use `FITNESS_TEST_POSTGRES_URL=postgresql+asyncpg://fitness:local-fitness-password@localhost:5432/fitness` only for the local Docker test database; never commit credentials or `.env`.

## Next task — start at Gate 4 only

Do not reopen Gates 1–3 unless a regression fails. Gate 4 remains entirely pending:

1. enqueue unique sync work immediately after confirmed opt-in;
2. keep public catalog refresh independent from private-data sync;
3. move recovery secret and bearer token behind a Keystore-protected storage abstraction with migration/reset behavior and test fake;
4. fix catalog loading/error/detail/cache-preservation states and move visible strings to resources;
5. add consent, Keystore, repository and UI-state tests.

After Gate 4, perform Gate 5 documentation consolidation and deterministic `make source-archive`. Do not begin the design system/App Shell (Gate 6) until Gates 4–5 are green and committed.
