# Diagrams

Mermaid diagrams are embedded in the Markdown architecture, data model, security and privacy documents so they remain reviewable and versioned with the text. Exported SVG/PNG files should be generated in CI and not hand-edited.
