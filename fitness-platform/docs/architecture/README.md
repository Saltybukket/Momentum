# Architecture details

Canonical high-level architecture is in the root `ARCHITECTURE.md`. This directory contains deeper, topic-specific architecture notes as the system grows. Current detailed contracts live under `docs/specifications/` and decisions under `docs/adr/`.
