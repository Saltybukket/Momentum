# Shared contracts

Generated or language-neutral contracts belong here. `openapi.json` is exported from the running FastAPI application via `make openapi`; Android DTOs remain explicit rather than generated in this first scaffold so contract drift is visible during review.
